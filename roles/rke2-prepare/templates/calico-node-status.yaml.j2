{% for host in groups['rke2'] %}
---
apiVersion: projectcalico.org/v3
kind: CalicoNodeStatus
metadata:
  name: "calico-node-{{ host }}"
spec:
  classes:
    - Agent
    - BGP
    - Routes
  node: "{{ host }}"
  updatePeriodSeconds: 10
{% endfor %}
